<div class="container py-5">
    <div class="row justify-content-center">
        <div class="col-md-6 col-lg-5">
            <div class="card shadow-lg border-0 rounded-3">
                <div class="card-body p-4">
                    <h2 class="text-center mb-4 fw-bold text-dark">
                        ➕ Adicionar Categoria
                    </h2>
                    <form action="/adicionar/categoria" method="post">
                        <div class="mb-3">
                            <label for="nome" class="form-label fw-semibold">Nome da categoria</label>
                            <input type="text" class="form-control form-control-lg" name="nome"
                                placeholder="Digite o nome da categoria" required pattern="[A-Za-zÀ-ÿ\s]+"
                                title="Digite apenas letras e espaços">
                        </div>
                        <button type="submit" class="btn btn-dark w-100 fw-bold">
                            <i class="bi bi-plus-circle"></i> Adicionar
                        </button>
                    </form>
                </div>
            </div>
        </div>
    </div>


    <div class="mt-5">
        <h3 class="text-center fw-bold mb-4 text-dark">📋 Categorias Cadastradas</h3>

        {{#if categorias}}
        <div class="table-responsive shadow rounded">
            <table class="table table-hover align-middle text-center">
                <thead class="table-dark">
                    <tr>
                        <th scope="col"></th>
                        <th scope="col">Nome da Categoria</th>
                        <th scope="col"></th>
                    </tr>
                </thead>
                <tbody>
                    {{#each categorias}}
                    <tr>
                        <td></td>
                        <td class="fw-semibold">{{this.nome_categoria}}</td>
                        <td>
                            <a href="/editar/categoria/{{this.id}}" class="btn btn-sm btn-warning me-2">
                                <i class="bi bi-pencil-square"></i> Editar
                            </a>
                            <a href="/deletar/categoria/{{this.id}}" class="btn btn-sm btn-danger"
                                onclick="return confirm('Tem certeza que deseja deletar esta categoria?')">
                                <i class="bi bi-trash"></i> Deletar
                            </a>
                        </td>
                    </tr>
                    {{/each}}
                </tbody>
            </table>
        </div>
        {{else}}
        <div class="alert alert-info text-center fw-semibold">
            📭 Nenhuma categoria cadastrada ainda.
        </div>
        {{/if}}
    </div>


    <div class="text-center mt-4">
        <a href="/painel/admin" class="btn btn-secondary">
            <i class="bi bi-arrow-left"></i> Voltar
        </a>
    </div>
</div>