<div class="container py-5">
    {{#if msg_success}}
    <div class="alert alert-success text-center mt-4 fw-bold" id="flash-message">
        Produto adicionado ao carrinho!
    </div>
    {{/if}}

    <h2 class="text-center fw-bold text-primary mb-4">🛒 Produtos à Venda</h2>

    <div class="row g-4">
        {{#each produtos}}
        <div class="col-md-4 col-lg-3">
            <div class="card shadow-sm border-0 h-100">
                <div class="card-body d-flex flex-column justify-content-between">
                    <h5 class="card-title text-dark fw-bold">{{this.nome_produto}}</h5>
                    <p class="card-text text-success fs-5 fw-semibold">R$ {{this.valor_produto}}</p>
                    <div class="mt-auto">
                        <a href="/adicionar/carrinho/{{this.id}}" class="btn btn-primary w-100">
                            ➕ Adicionar ao Carrinho
                        </a>
                    </div>
                </div>
            </div>
        </div>
        {{/each}}
    </div>

    <div class="text-center mt-4">
        <a href="/painel/usuario" class="btn btn-outline-secondary">
            🔙 Voltar ao Painel
        </a>
    </div>
</div>
<script>
    setTimeout(() => {
        const flash = document.getElementById("flash-message");

        if (flash) {

            setTimeout(() => flash.remove(), 500);
        }

    }, 3000)
</script>